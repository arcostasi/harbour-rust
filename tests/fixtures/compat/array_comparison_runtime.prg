PROCEDURE Main()
   LOCAL same := { 1 }
   LOCAL left := {}
   LOCAL right := {}

   ? same == same
   ? left == right
   ? same = same
   ? same != same
   ? same < same
   ? same <= same
   ? same > same
   ? same >= same
RETURN
