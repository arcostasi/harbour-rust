PROCEDURE Main()
   ? SubStr( "abcdef", 0, 1 )
   ? SubStr( "abcdef", 2, 7 )
   ? SubStr( "abcdef", -2 )
   ? SubStr( "abcdef", 10 )
   ? SubStr( "abcdef", 2, -1 )
   ? SubStr( 100, 0, -1 )
   ? SubStr( "abcdef", "a" )
RETURN
