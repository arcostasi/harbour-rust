PROCEDURE Main()
   LOCAL high := 10
   LOCAL low := 2

   IF high > 5
      ? "maior"
   ELSE
      ? "falhou"
   ENDIF

   IF low > 5
      ? "falhou"
   ELSE
      ? "menor ou igual"
   ENDIF
RETURN
