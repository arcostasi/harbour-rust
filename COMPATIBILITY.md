# COMPATIBILITY

Status:

- `planned`: ainda não iniciado
- `partial`: implementado parcialmente
- `done`: coberto e testado
- `deferred`: adiado para release futura

## Matriz viva

| Recurso | Dialeto | Status | Fase | Oráculo upstream | Teste esperado | Notas |
| --- | --- | --- | --- | --- | --- | --- |
| `PROCEDURE Main()` | Clipper | partial | 2/5 | `tests/hello.prg` | parser + snapshot + cli run | sintaxe procedural mínima e AST cobertas na Fase 2; `hello.prg` já compila e executa via CLI na Fase 5; cobertura ainda parcial fora do caminho feliz |
| `FUNCTION` | Clipper | partial | 2/3 | `src/compiler/harbour.y` | unit + snapshot + sema | assinatura simples parseia; resolução básica de chamadas por nome coberta na Fase 3 |
| `LOCAL` | Clipper | partial | 2/3 | `harbour.y` | parser + sema | sintaxe e AST cobertas na Fase 2; resolução básica de parâmetros e `LOCAL` coberta na Fase 3 |
| `RETURN` | Clipper | partial | 2/4/5 | `tests/returns.prg` | parser + snapshot + runtime + cli run | `RETURN` vazio e com expressão parseiam; valores de retorno e surface de runtime existem; `RETURN` vazio participa do caminho executável de `hello.prg` na Fase 5 |
| `?` / `QOut()` | Clipper | partial | 2/4/5 | `doc/pcode.txt`, `tests/hello.prg` | parser + runtime + cli run | sintaxe de `?` coberta na Fase 2; formatter, `QOut()` mínimo e dispatch case-insensitive por nome entram na Fase 4; `hello.prg` já executa `QOut()` via compilador C host na Fase 5 |
| `IF / ELSE / ENDIF` | Clipper | partial | 2/7 | `harbour.y` | parser + codegen-c + cli run | sintaxe e AST cobertas na Fase 2; `tests/fixtures/parser/if_else.prg` agora cobre o primeiro caminho executável no backend C e no `cli run`; `ELSEIF`, combinações mais amplas de expressão e regressões em programas maiores continuam pendentes |
| `DO WHILE / ENDDO` | Clipper | partial | 2/5 | `tests/while.prg` | integração + cli run | sintaxe e AST cobertas na Fase 2; `while.prg` já compila e executa no caminho inicial da Fase 5; outras formas de condição e corpo ainda pendentes |
| `FOR / NEXT` | Clipper | partial | 2/5 | `harbour.y`, `tests/fornext.prg` | integração + cli run | sintaxe e AST cobertas na Fase 2; `for_sum.prg` já compila e executa no caminho inicial da Fase 5; sem `FOR EACH`, sem passo negativo e sem variantes mais amplas |
| operadores básicos | Clipper | partial | 1/2/3/4/5/7 | `ppcore.c`, `harbour.y` | unit + parser + sema + runtime + cli run | tokenização base e multi-caractere cobertas na Fase 1; parsing e resolução básica cobertas até a Fase 3; aritmética e comparação básicas cobertas no runtime da Fase 4; `<`, `<=`, `+` e `x++` entram no caminho executável inicial da Fase 5; `+= -= *= /=` agora atravessam `parser -> hir -> ir -> codegen-c -> cli run` para alvos nominais simples; `%= ^=` continuam apenas no parsing/lowering com erro explícito de codegen nesta fase |
| strings | Clipper | partial | 1/4/7 | `doc/pp.txt`, `utils/hbtest/rt_hvm.prg`, `utils/hbtest/rt_str.prg` | unit + runtime + compat + cli run | literais e erro de string não terminada cobertos na Fase 1; concatenação e saída orientada a print entram na Fase 4; na Fase 7, `=` passa a seguir o baseline padrão de `SET EXACT OFF` para strings, com comparação por prefixo direcional (`left.starts_with(right)`), `==` permanece estrito e `!=` nega a semântica de `=`; `tests/fixtures/compat/string_compare_runtime.out` e `tests/fixtures/parser/string_compare.prg` fixam esse recorte contra `utils/hbtest/rt_hvm.prg`; toggle real de `SET EXACT` e semântica mais ampla por dialeto continuam pendentes; `[]` segue para PP |
| números | Clipper | partial | 1/4 | `doc/pp.txt` | unit + runtime | inteiros e floats decimais tokenizados na Fase 1; aritmética, promoção numérica e formatação básica entram na Fase 4; corner cases ainda pendentes |
| comentários | Clipper | partial | 1 | `tests/hello.prg`, `tests/while.prg` | unit + golden | `//`, `&&`, `/* */` e `*` em início de linha cobertos pelo lexer |
| `STATIC` | Clipper | partial | 7 | `doc/statics.txt`, `tests/statics*.prg` | parser + hir + sema + ir + codegen-c + cli run | `STATIC` já parseia com lista inicial de inicializadores tanto em rotina quanto no nível de módulo, baixa para HIR e IR como nós explícitos, e leituras nominais entram como `Read(path)` nas duas camadas; a sema agora aceita declarações `STATIC`, resolve leituras/escritas nominais no mesmo routine e também bindings de módulo compartilhados entre rotinas do mesmo arquivo; o `codegen-c` já gera storage C persistente por rotina com guard de inicialização e também storage global compartilhado com helper `harbour_module_init_statics()`, e `tests/fixtures/parser/static_counter.prg` e `tests/fixtures/parser/static_module.prg` validam esse baseline via `harbour-rust-cli run`; semântica mais ampla de inicialização entre módulos, dialetos e runtime avançado continua pendente |
| arrays | Clipper | partial | 7 | `src/vm/arrays.c`, `utils/hbtest/rt_hvm.prg`, `utils/hbtest/rt_hvma.prg`, `utils/hbtest/rt_array.prg` | parser + hir + runtime + compat + cli run | `Value::Array` e construtores básicos já existem no runtime; literais `{}` e `{ expr, ... }` já parseiam e agora baixam para HIR e IR explícitas; `expr[expr]`, `expr[expr, ...]` e encadeamento como `expr[expr][expr]` também já baixam para HIR e IR explícitas na Fase 7; o runtime Rust já expõe leitura e escrita 1-based com diagnóstico de bounds, tipo e caminho vazio; o backend C agora emite `harbour_value_from_array_items(...)`, `harbour_value_array_get(...)`, `harbour_value_array_set_path(...)`, `harbour_value_exact_equals(...)`, `harbour_value_equals(...)`, `harbour_value_not_equals(...)`, `harbour_value_less_than(...)`, `harbour_value_less_than_or_equal(...)`, `harbour_value_greater_than(...)` e `harbour_value_greater_than_or_equal(...)`, e o suporte host já materializa `Array`, leitura e escrita indexadas 1-based, identidade observável de arrays para `==` e mensagens observáveis `BASE 1071` a `1076` no caminho `cli run`; `exact_equals()` já modela o baseline observável de `==` para arrays por identidade do valor observado, `=`/`<>` e ordenação com arrays agora já usam mensagens/códigos mais próximos do baseline xBase (`1071` a `1076`), e o fixture focado `tests/fixtures/compat/array_comparison_runtime.prg` agora fixa esse baseline diretamente contra `utils/hbtest/rt_hvm.prg` na surface pública do runtime; `tests/fixtures/parser/array_exact_compare.prg` agora também valida `==` de arrays no caminho executável `cli run`, `tests/fixtures/parser/compare_ops.prg` valida os diagnósticos executáveis de `=`/`<>`/`>`/`>=`, e `tests/fixtures/parser/compare_ops_lt.prg` valida `<`/`<=`; `tests/fixtures/parser/array_args.prg` agora valida passagem de array como argumento de rotina no caminho executável, e `tests/fixtures/parser/array_matrix.prg` valida leitura/escrita multidimensional com `Len()` sobre subarray; `aadd()` e `asize()` já existem sobre `array_push()`/`array_resize()` via `call_builtin_mut()`, `aclone()` agora expõe `AClone()` na surface imutável sobre `array_clone()`, e `adel()`/`ains()`/`ascan()` já cobrem o recorte mínimo de builtins de array no runtime, no `codegen-c`, no `cli run` e no baseline focado `tests/fixtures/compat/array_builtins_runtime.prg`; `AScan()` segue o baseline leniente atual de prefixo para strings com `SET EXACT OFF`, enquanto `ADel()` e `AIns()` preservam o comprimento do array e tratam `0` como posição `1`; `AClone()` já atravessa `codegen-c` e `cli run` via `harbour_builtin_aclone(...)`, e agora `AAdd()`/`ASize()` também atravessam `codegen-c` e `cli run` quando o primeiro argumento é símbolo endereçável simples, via `harbour_builtin_aadd(&value, ...)` e `harbour_builtin_asize(&value, ...)`; primeiros argumentos não endereçáveis continuam com erro explícito de codegen nesta fase; leitura e escrita indexadas agora já usam mensagens/códigos mais próximos do baseline xBase (`1068/1069/1132/1133`); semântica mais ampla para objetos, codeblocks e outras combinações continua pendente |
| `Len()` | Clipper | partial | 7 | `src/rtl/len.c`, `utils/hbtest/rt_hvma.prg` | runtime + compat + codegen-c + cli run | `Len()` agora retorna tamanho para `String` e `Array` no runtime Rust, no runtime host C e no dispatch do `codegen-c`; `tests/fixtures/parser/len_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/len_runtime.out` ancora o baseline observado para `String`, `Array`, `NIL` e `Integer`; hashes, objetos e contagem por codepage multibyte continuam pendentes |
| `SubStr()` | Clipper | partial | 7 | `src/rtl/substr.c`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `SubStr()` agora cobre o baseline inicial para `String` com `start` e `count` inteiros, incluindo `0`, negativos, clipping fora da faixa e erro `BASE 1110` para argumentos inválidos; `tests/fixtures/parser/substr_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/substr_runtime.out` ancora o recorte observado de `rt_str.prg`; codepage/multibyte, floats em `start/count` e suporte a bytes com `Chr(0)` no runtime host C continuam pendentes |
| `Left()` | Clipper | partial | 7 | `src/rtl/left.c`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `Left()` agora cobre o baseline inicial para `String` com `count` inteiro, incluindo `<= 0`, clipping no tamanho da string e erro `BASE 1124` para argumentos inválidos; `tests/fixtures/parser/left_right_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/left_right_runtime.out` ancora o recorte observado de `rt_str.prg`; codepage/multibyte e semântica completa fora de strings ASCII continuam pendentes |
| `Right()` | Clipper | partial | 7 | `src/rtl/right.c`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `Right()` agora cobre o baseline inicial para `String` com `count` inteiro, incluindo `<= 0`, clipping no tamanho da string e o recorte leniente observado em `rt_str.prg` para argumentos inválidos retornando string vazia; `tests/fixtures/parser/left_right_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/left_right_runtime.out` ancora esse baseline; codepage/multibyte e semântica completa fora de strings ASCII continuam pendentes |
| `Upper()` | Clipper | partial | 7 | `src/rtl/strcase.c`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `Upper()` agora cobre o baseline inicial ASCII para `String`, incluindo string vazia e erro `BASE 1102` para argumentos inválidos; `tests/fixtures/parser/upper_lower_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/upper_lower_runtime.out` ancora o recorte observado de `rt_str.prg`; `Chr(0)`, codepage/multibyte e by-ref continuam pendentes |
| `Lower()` | Clipper | partial | 7 | `src/rtl/strcase.c`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `Lower()` agora cobre o baseline inicial ASCII para `String`, incluindo string vazia e erro `BASE 1103` para argumentos inválidos; `tests/fixtures/parser/upper_lower_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/upper_lower_runtime.out` ancora o recorte observado de `rt_str.prg`; `Chr(0)`, codepage/multibyte e by-ref continuam pendentes |
| `Trim()` / `RTrim()` | Clipper | partial | 7 | `src/rtl/trim.c`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `Trim()` agora segue o baseline histórico de sinônimo de `RTrim()`, removendo apenas espaços `' '` à direita e emitindo `BASE 1100 Argument error (TRIM)` para argumentos inválidos; `tests/fixtures/parser/trim_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/trim_runtime.out` ancora o recorte observado; `Chr(0)`, by-ref e extensões Harbour como segundo parâmetro continuam pendentes |
| `LTrim()` | Clipper | partial | 7 | `src/rtl/trim.c`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `LTrim()` agora remove whitespace ASCII à esquerda no recorte atual e emite `BASE 1101 Argument error (LTRIM)` para argumentos inválidos; `tests/fixtures/parser/trim_builtin.prg` e `tests/fixtures/compat/trim_runtime.out` cobrem o baseline inicial observado; regras completas de whitespace não-ASCII, `Chr(0)` e by-ref continuam pendentes |
| `At()` | Clipper | partial | 7 | `src/rtl/at.c`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `At()` agora cobre o recorte clássico de dois argumentos string com retorno 1-based, `0` quando não encontra e `BASE 1108 Argument error (AT)` para argumentos inválidos; `tests/fixtures/parser/at_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/at_runtime.out` ancora o baseline observado de `rt_str.prg`; codepage/multibyte, `hb_AT()` com `start/to` e divergências históricas do otimizador Clipper para string vazia continuam pendentes |
| `Replicate()` | Clipper | partial | 7 | `src/rtl/str.c`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `Replicate()` agora cobre o recorte inicial de string com contagem numérica inteira ou float truncada, incluindo `<= 0` retornando string vazia e `BASE 1106 Argument error (REPLICATE)` para argumentos inválidos; `tests/fixtures/parser/replicate_space_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/replicate_space_runtime.out` ancora o baseline observado de `rt_str.prg`; `Chr(0)`, overflow completo do upstream e semântica de codepage multibyte continuam pendentes |
| `Space()` | Clipper | partial | 7 | `src/rtl/str.c`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `Space()` agora cobre o recorte inicial com tamanho numérico inteiro ou float truncado, incluindo `<= 0` retornando string vazia e `BASE 1105 Argument error (SPACE)` para argumentos inválidos; `tests/fixtures/parser/replicate_space_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/replicate_space_runtime.out` ancora o baseline observado de `rt_str.prg`; overflow completo do upstream e semântica de codepage multibyte continuam pendentes |
| `Abs()` | Clipper | partial | 7 | `src/rtl/math.c`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Abs()` agora cobre o recorte inicial numérico para `Integer` e `Float`, preservando a forma do número e emitindo `BASE 1089` para argumentos inválidos; `tests/fixtures/parser/abs_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/abs_runtime.out` ancora o baseline observado de `rt_math.prg`; by-ref, handlers matemáticos do upstream e corner cases extremos além desse recorte continuam pendentes |
| `Sqrt()` | Clipper | partial | 7 | `src/rtl/math.c`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Sqrt()` agora cobre o recorte inicial numérico para `Integer` e `Float`, retornando `0` para valores `<= 0` e emitindo `BASE 1097` para argumentos inválidos; `tests/fixtures/parser/sqrt_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/sqrt_runtime.out` ancora o baseline observado de `rt_math.prg`; handlers matemáticos do upstream, precisão/escala histórica completa e corner cases mais profundos continuam pendentes |
| `Exp()` | Clipper | partial | 7 | `src/rtl/math.c`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Exp()` agora cobre o recorte inicial numérico para `Integer` e `Float`, emitindo `BASE 1096` para argumentos inválidos e preservando o baseline observável de `Exp(0)`, `Round( Exp(1), 2 )` e `Str( Exp(1), 20, 10 )`; `tests/fixtures/parser/exp_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/exp_runtime.out` ancora o recorte observado de `rt_math.prg`; `Str( Exp(15) )` ainda diverge do `harbour-core` porque reaproveita a largura/escala simplificada do `Str()` atual; handlers matemáticos do upstream, substituição de erro histórica completa e corner cases mais profundos continuam pendentes |
| `Sin()` | Clipper | partial | 7 | `src/rtl/math.c`, `doc/c_std.txt` | runtime + compat + codegen-c + cli run | `Sin()` agora cobre o recorte inicial numérico para `Integer` e `Float`, usando `sin()` sobre `f64`, com `BASE 1091` para argumentos inválidos; `tests/fixtures/parser/sin_cos_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/sin_cos_runtime.out` fixa o baseline atual; o checkout local do upstream não traz um oracle direto em `utils/hbtest` para `Sin()`, então esta slice usa baseline provisório documentado e ainda deve ser refinada quando houver fixture observável melhor |
| `Cos()` | Clipper | partial | 7 | `src/rtl/math.c`, `doc/c_std.txt` | runtime + compat + codegen-c + cli run | `Cos()` agora cobre o recorte inicial numérico para `Integer` e `Float`, usando `cos()` sobre `f64`, com `BASE 1091` para argumentos inválidos; `tests/fixtures/parser/sin_cos_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/sin_cos_runtime.out` fixa o baseline atual; o checkout local do upstream não traz um oracle direto em `utils/hbtest` para `Cos()`, então esta slice usa baseline provisório documentado e ainda deve ser refinada quando houver fixture observável melhor |
| `Tan()` | Clipper | partial | 7 | `contrib/hbct/trig.c`, `contrib/hbct/tests/test.prg`, `contrib/hbct/doc/en/trig.txt` | runtime + compat + codegen-c + cli run | `Tan()` agora cobre o recorte inicial numérico para `Integer` e `Float`, usando `tan()` sobre `f64`, com `Tan(0) = 0` e `Round( Tan(1), 4 ) = 1.5574` ancorados no CT3/hbct local; `tests/fixtures/parser/tan_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/tan_runtime.out` fixa o baseline atual; a política de erro ainda segue a surface xBase-style do runtime atual com `BASE 1091`, enquanto o upstream hbct usa `ct_error_subst()`, então essa divergência continua documentada |
| `Log()` | Clipper | partial | 7 | `src/rtl/math.c`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Log()` agora cobre o recorte inicial numérico para `Integer` e `Float`, retornando `-infinity` para valores `<= 0`, emitindo `BASE 1095` para argumentos inválidos e preservando o baseline observável de `Str( Log( -1 ) )` com placeholder de overflow em compat/runtime; `tests/fixtures/parser/log_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/log_runtime.out` ancora o recorte observado de `rt_math.prg`; handlers matemáticos do upstream, `Exp()` e corner cases mais profundos continuam pendentes |
| `Int()` | Clipper | partial | 7 | `src/rtl/math.c`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Int()` agora cobre o recorte inicial numérico com truncamento toward-zero para `Float`, preserva `Integer` como inteiro e emite `BASE 1090` para argumentos inválidos; `tests/fixtures/parser/int_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/int_runtime.out` ancora o baseline observado de `rt_math.prg`; by-ref, overflow extremo e corner cases mais profundos do upstream continuam pendentes |
| `Round()` | Clipper | partial | 7 | `src/rtl/round.c`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Round()` agora cobre o recorte inicial numérico com `nDecimals` obrigatório e numérico, arredondamento half-away-from-zero, suporte a decimais negativos e erro `BASE 1094` para argumentos inválidos; `tests/fixtures/parser/round_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/round_runtime.out` ancora o baseline observado de `rt_math.prg`; escala histórica exata do item numérico, zeros à direita do upstream, by-ref e corner cases mais profundos continuam pendentes |
| `Mod()` | Clipper | partial | 7 | `src/rtl/mod.c`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Mod()` agora cobre o recorte inicial numérico com resto ajustado ao sinal do divisor, terceiro argumento ignorado como no upstream, erro `BASE 1085` para argumentos inválidos e `BASE 1341` para divisor zero; `tests/fixtures/parser/mod_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/mod_runtime.out` ancora o baseline observado de `rt_math.prg`; preservação histórica exata da representação numérica e do item original em substituição de erro continuam pendentes |
| `Max()` | Clipper | partial | 7 | `src/rtl/minmax.c`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Max()` agora cobre o recorte inicial para `Integer`, `Float` e `Logical`, preservando o primeiro argumento em caso de empate observável nesse subset e emitindo `BASE 1093` para argumentos inválidos; `tests/fixtures/parser/max_min_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/max_min_runtime.out` ancora o baseline escolhido contra `rt_math.prg`; datas, datetime, by-ref e demais tipos do upstream continuam pendentes |
| `Min()` | Clipper | partial | 7 | `src/rtl/minmax.c`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Min()` agora cobre o recorte inicial para `Integer`, `Float` e `Logical`, preservando o primeiro argumento em caso de empate observável nesse subset e emitindo `BASE 1092` para argumentos inválidos; `tests/fixtures/parser/max_min_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/max_min_runtime.out` ancora o baseline escolhido contra `rt_math.prg`; datas, datetime, by-ref e demais tipos do upstream continuam pendentes |
| `Str()` | Clipper | partial | 7 | `src/rtl/str.c`, `utils/hbtest/rt_stra.prg`, `utils/hbtest/rt_hvma.prg`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Str()` agora cobre o recorte inicial numérico com largura e decimais opcionais, `BASE 1099` para argumentos inválidos e overflow explícito por largura com `*****`; `tests/fixtures/parser/str_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/str_runtime.out` ancora o baseline escolhido contra `rt_stra.prg`, `rt_hvma.prg` e `rt_math.prg`; a semântica histórica completa de precisão/escala herdada do upstream, larguras negativas e codepages continua pendente |
| `Val()` | Clipper | partial | 7 | `src/rtl/val.c`, `utils/hbtest/rt_str.prg`, `utils/hbtest/rt_math.prg` | runtime + compat + codegen-c + cli run | `Val()` agora cobre o recorte inicial de parsing string->número com whitespace ASCII à esquerda, sinal simples, inteiro/decimal básico, lixo à direita e `BASE 1098` para argumentos não-string; `tests/fixtures/parser/val_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/val_runtime.out` ancora o baseline escolhido contra `rt_str.prg` e `rt_math.prg`; exponentes, `Chr(0)`, pontos repetidos e corner cases mais profundos do upstream continuam pendentes |
| `ValType()` | Clipper | partial | 7/8 | `src/rtl/valtype.c`, `utils/hbtest/rt_hvm.prg` | runtime + compat + codegen-c + cli run | `ValType()` agora cobre os tipos já materializados no runtime atual, com `U/L/N/C/A/B` para `NIL`, `Logical`, `Integer/Float`, `String`, `Array` e `Codeblock`; o runtime host C já materializa `Codeblock` o bastante para `Eval()` e `ValType()`, mas a cobertura executável ainda segue parcial e focada no subset de codeblocks não-capturantes; datas, objetos, memos e outros tipos ainda fora do runtime seguem pendentes |
| `Type()` | Clipper | partial | 7 | `src/rtl/type.c`, `utils/hbtest/rt_hvm.prg` | runtime + compat + codegen-c + cli run | `Type()` agora cobre um recorte textual mínimo e explícito: ausência de argumento ou argumento não-string emite `BASE 1121`, e strings de origem como `NIL`, `.T./.F.`, números simples, literais `{...}` e strings quoted retornam `U/L/N/A/C`; nomes textuais não resolvidos retornam `U`; `tests/fixtures/parser/type_builtin.prg` cobre o caminho executável e `tests/fixtures/compat/type_runtime.out` ancora esse baseline parcial contra `rt_hvm.prg`; macro evaluation completa, resolução de nomes, datas, objetos, codeblocks e demais tipos do upstream continuam pendentes |
| `Empty()` | Clipper | partial | 7/8 | `src/rtl/empty.c`, `utils/hbtest/rt_hvma.prg` | runtime + compat + codegen-c + cli run | `Empty()` agora cobre o recorte atual para `NIL`, `Logical`, `Integer/Float`, `String`, `Array` e `Codeblock`, com baseline leniente alinhado ao upstream: `NIL`, `.F.`, `0`, `0.0`, strings compostas apenas por whitespace ASCII e arrays vazios retornam `.T.`; codeblocks retornam `.F.` no runtime Rust e no subset executável host C da Fase 8; `tests/fixtures/parser/empty_builtin.prg` cobre o caminho executável do subset anterior e `tests/fixtures/compat/empty_runtime.out` ancora o recorte observado em `rt_hvma.prg`; datas, pointers, hashes e objetos continuam pendentes |
| builtins de string | Clipper | partial | 7 | `src/rtl`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `Len`, `SubStr`, `Left`, `Right`, `Upper`, `Lower`, `Trim`, `LTrim`, `RTrim`, `At`, `Replicate` e `Space` já têm baseline executável e compat focada; `StrTran()` e `Pad*()` seguem pendentes |
| builtins matemáticos e conversão | Clipper | partial | 7 | `utils/hbtest/rt_math.prg`, `utils/hbtest/rt_hvm*.prg`, `utils/hbtest/rt_str.prg` | runtime + compat + codegen-c + cli run | `Abs`, `Int`, `Round`, `Mod`, `Val`, `Str`, `ValType`, `Type`, `Empty`, `Max`, `Min`, `Sqrt`, `Log`, `Exp`, `Sin`, `Cos` e `Tan` já têm baseline executável; várias superfícies ainda seguem parciais e documentadas por builtin |
| `#define` | Clipper | partial | 6 | `doc/pp.txt`, `tests/pp.prg` | unit + integração | parsing inicial de diretiva, registro de defines e expansão recursiva case-insensitive de macros objeto em linhas normais, com diagnóstico de ciclo; macros parametrizadas e expansão token-based ainda pendentes |
| `#include` | Clipper | partial | 6 | `ppcore.c` | integração + cli build/run | resolução inicial relativa ao arquivo atual, fallback por search paths configuráveis, suporte inicial a `<...>` e handoff `pp -> parser` no CLI; spans finos e política completa de busca ainda pendentes |
| `#command` | Clipper | planned | 9 | `tests/hbpp/_pp_test.prg` | compat | implementação incremental |
| `#translate` | Clipper | planned | 9 | `tests/hbpp/_pp_test.prg` | compat | |
| `PRIVATE` | xBase | partial | 8 | `src/vm/memvars.c`, `tests/memvar.prg` | parser + hir + ir + runtime + compat + cli run | sintaxe de declaração com inicializadores opcionais já parseia em `tests/fixtures/parser/memvars.prg`; HIR e IR preservam `Statement::Private` explicitamente; o runtime Rust já mantém frames privados dinâmicos com precedência sobre `PUBLIC`; o runtime host C agora também empilha frame privado por rotina e `tests/fixtures/parser/private_dynamic.prg` já valida o caminho executável com atualização dinâmica entre rotinas; `tests/fixtures/compat/phase8_dynamic_runtime.out` fixa o baseline mínimo de precedência `PUBLIC -> PRIVATE` e `assign/pop` ancorado em `harbour-core/tests/memvar.prg`; escopos mais profundos com codeblocks destacados, by-reference e macro assignment seguem pendentes |
| `PUBLIC` | xBase | partial | 8 | `src/vm/memvars.c`, `tests/memvar.prg` | parser + hir + ir + runtime + compat + cli run | sintaxe de declaração com inicializadores opcionais já parseia em `tests/fixtures/parser/memvars.prg`; HIR e IR preservam `Statement::Public` explicitamente; o runtime Rust já mantém tabela pública dinâmica compartilhada; o runtime host C agora também materializa storage público global e `tests/fixtures/parser/public_dynamic.prg` valida o caminho executável com atualização entre rotinas; `tests/fixtures/compat/phase8_dynamic_runtime.out` fixa o fallback para `PUBLIC` depois do `pop` do frame privado; serialização entre módulos e superfícies mais amplas seguem pendentes |
| memvars | xBase | partial | 8 | `src/vm/memvars.c` | parser + sema + hir + ir + runtime + compat + cli run | surface sintática inicial estabilizada via `PRIVATE`/`PUBLIC`; a sema registra memvars explícitas e já permite fallback dinâmico entre rotinas quando o programa entra em modo xBase dinâmico; a IR distingue `ReadPath::Memvar` e `AssignTarget::Memvar` em fixtures como `tests/fixtures/parser/private_dynamic.prg`; o runtime Rust implementa leitura e atribuição dinâmicas com prioridade `PRIVATE -> PUBLIC`; o backend C agora já baixa declaração, leitura e atribuição de memvar para helpers explícitos de runtime, e `private_dynamic.prg`/`public_dynamic.prg` cobrem o baseline executável; `tests/fixtures/compat/phase8_dynamic_runtime.out` agora ancora o recorte mínimo no runtime Rust contra `harbour-core/tests/memvar.prg`, e `tests/fixtures/parser/phase8_acceptance.prg` cobre esse mesmo recorte no caminho completo `cli run`; macro assignment, detach completo e superfícies mais amplas seguem pendentes |
| codeblocks | Clipper/Harbour | partial | 8 | `src/vm/codebloc.c`, `doc/codebloc.txt` | parser + sema + hir + ir + runtime + compat + cli run | literais `{|| ...}` e `{|x, y| ...}` já parseiam em `tests/fixtures/parser/codeblock.prg`; a sema já trata parâmetros de codeblock como escopo aninhado; HIR e IR preservam `Codeblock` explicitamente, inclusive com atribuição em posição de expressão; o runtime Rust materializa `Value::Codeblock`, `ValType() -> "B"` e `Eval()` mínimo; o backend C agora já executa `Eval()` para codeblocks não-capturantes e codeblocks que leem memvars, com baselines em `tests/fixtures/parser/eval_codeblock.prg`, `tests/fixtures/parser/eval_memvar_codeblock.prg` e `tests/fixtures/parser/phase8_acceptance.prg`; captura lexical de `LOCAL` externo continua com erro explícito de codegen em `tests/fixtures/parser/eval_capture_error.prg` |
| macro operator | Harbour | partial | 8/9 | `src/vm/macro.c`, `tests/macro.prg` | parser + hir + ir + snapshot + cli run | macro read inicial `&name` e `&(expr)` já parseia em `tests/fixtures/parser/macro_read.prg`; HIR e IR preservam `Macro` explicitamente; o backend C agora já baixa a leitura dinâmica mínima para `harbour_macro_read(...)`, e `tests/fixtures/parser/macro_memvar.prg` e `tests/fixtures/parser/phase8_acceptance.prg` validam `&name`/`&(expr)` como lookup de memvar no caminho `cli run`; callable macro, macro assignment e forms mais amplas continuam pendentes |
| `FOR EACH` | Harbour | deferred | pós-0.2 | `doc/clipper.txt` | compat | extensão Harbour, não baseline |
| `WITH OBJECT` | Harbour | deferred | pós-0.2 | `doc/clipper.txt` | compat | |
| RDD/DBF | Clipper/Harbour | planned | 10 | `src/rdd`, `tests/rddtest` | integração | só após frontend estável |

## Marco atual

O aceite da Fase 5 está fechado com o pipeline procedural fim a fim:

- `examples/hello.prg` compila e executa via CLI,
- `tests/fixtures/parser/while.prg` cobre o primeiro caminho executável com `DO WHILE`,
- `tests/fixtures/parser/for_sum.prg` cobre o primeiro caminho executável com `FOR` simples.

O aceite da Fase 6 está fechado com o baseline inicial de pré-processamento:

- `#define` objeto já expande em linhas normais, inclusive de forma recursiva,
- ciclos de macro objeto geram diagnóstico explícito,
- `#include "..."` e `#include <...>` já entram no caminho `pp -> parser` do CLI com `-I/--include-dir`.

O aceite da Fase 7 está fechado com compatibilidade procedural ampliada:

- `tests/fixtures/parser/phase7_acceptance.prg` compila e executa com `IF`, `FOR`, `STATIC`, arrays e builtins essenciais,
- `STATIC` same-routine e de módulo já atravessam `parser -> hir -> sema -> ir -> codegen-c -> cli run`,
- arrays, builtins essenciais de string, builtins essenciais de math/conversão e operadores compostos já têm baseline executável,
- a matriz acima passa a registrar as limitações remanescentes builtin a builtin, em vez de tratar a fase como não concluída.

O baseline atual da Fase 8 já cobre o primeiro recorte executável de semântica dinâmica:

- `tests/fixtures/parser/private_dynamic.prg` valida `PRIVATE` dinâmica entre rotinas,
- `tests/fixtures/parser/public_dynamic.prg` valida `PUBLIC` compartilhado,
- `tests/fixtures/parser/macro_memvar.prg` valida `&name` / `&(expr)` como macro read mínima de memvar,
- `tests/fixtures/parser/eval_codeblock.prg` valida `Eval()` com codeblock não-capturante,
- `tests/fixtures/parser/eval_memvar_codeblock.prg` valida `Eval()` com codeblock lendo memvar,
- `tests/fixtures/parser/eval_capture_error.prg` fixa o erro explícito de captura lexical ainda não suportada no backend C.

O aceite da Fase 8 está fechado com compatibilidade dinâmica xBase no recorte escolhido:

- `tests/fixtures/parser/phase8_acceptance.prg` compila e executa com `PRIVATE`, `PUBLIC`, memvars dinâmicas entre rotinas, `Eval()` e macro read mínima,
- `tests/fixtures/compat/phase8_dynamic_runtime.out` ancora o baseline focado de memvars e codeblocks contra `harbour-core/tests/memvar.prg`, `harbour-core/doc/codebloc.txt` e `harbour-core/src/vm/codebloc.c`,
- captura lexical de `LOCAL` em codeblocks permanece documentada como divergência conhecida e coberta por erro explícito de codegen em `tests/fixtures/parser/eval_capture_error.prg`,
- a matriz acima passa a registrar as limitações remanescentes de memvars, codeblocks e macro operator sem manter a fase em aberto.

## Regras

- Nenhum recurso vai para `done` sem teste automatizado.
- `partial` exige nota de limitação e fixture explícita.
- Toda divergência observada contra `harbour-core` deve ser registrada aqui.
